[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers -m "not browser and not network"
markers =
    browser: requires a running server and a Playwright browser (deselected by default; run with -m browser)
    network: reaches a third-party service (deselected by default; run with -m network)
