# Demand forecasting pipeline (solution.py, post_process.py).
# Not needed to run the Command Center — see requirements.txt for that.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11

# Gradient boosting engines. Each is imported lazily inside its training
# block, so the pipeline still runs with any subset installed — the blend
# weights adapt to whichever engines produced predictions.
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
